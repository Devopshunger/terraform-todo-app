name: Runner Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-runner:
    runs-on:
      group: Default

    steps:
      - name: Test Runner
        shell: powershell
        run: |
          Write-Host "Runner is working!"
          hostname
          whoami
          Get-Location
          
